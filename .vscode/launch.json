{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "CUDA Debugger (cuda-gdb)",
            "type": "cuda-gdb",
            "request": "launch",
            "program": "${workspaceFolder}/build/prog",
            "cwd": "${workspaceFolder}",
            "stopAtEntry": false
        }
    ]
}